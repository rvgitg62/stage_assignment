name: story_api
description: api interfaces to implement story board api in stage application
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ^3.22.0

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.0
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
  json_serializable: ^6.3.1
